# 微信小程序运维交接清单（GooMooTechWXProgram / 橘猫智酷商城小程序）

> 用途：把本项目的运维工作逐步交接给“小微 / wx-ops”。
> 
> 已参考现有文档：
> - `docs/02-系统架构与设计文档.md`
> - `docs/04-部署与运维手册.md`
> - `docs/05-环境与配置说明.md`
> 
> 填写原则：先写清楚“去哪看、怎么做、谁负责”，不要一开始追求完美。

---

## 0. 项目基础信息

- **项目名称：** 橘猫智酷商城小程序
- **项目简介：** 微信小程序电商与内容展示一体化项目，包含商品、贩售、购物车、订单、支付、资讯、客服、积分等能力
- **仓库地址：** https://github.com/jackcry150/GooMooTechWXProgram
- **主分支：** main
- **测试分支：**
- **当前负责人：**
- **技术栈概览：**
  - 小程序端：UniApp（Vue 2）→ 编译为微信小程序
  - 后端：ThinkPHP 8 多应用（api + adm）
  - 数据库：MySQL 5.7+
  - 云服务：
  - 第三方服务：汇付 / 微信支付、管家婆、企业微信客服

---

## 1. 项目目录说明

> 让接手的人快速知道关键代码在哪。

- **前端主目录：** `uniapp_623/`
- **接口请求相关目录：** `uniapp_623/utils/request.js`
- **配置文件目录：** `php/config/`
- **服务端目录：** `php/`
- **后端 API 控制器：** `php/app/api/controller/`
- **后台管理控制器：** `php/app/adm/controller/`
- **数据库脚本：** `php/database/schema_optimized.sql`
- **静态资源目录：** `php/public/static/`、`uniapp_623/static/`
- **上传文件目录：** `php/public/uploads/`
- **日志/运行时目录：** `php/runtime/`
- **docs 文档目录：** `docs/`
- **其他重要目录说明：** Web 根目录必须指向 `php/public`，不能直接指向 `php/`

---

## 2. 环境说明

### 2.1 环境划分

- **本地开发环境：**
- **测试环境：**
- **预发环境：**
- **正式环境：**

### 2.2 各环境对应关系

| 环境 | 分支 | 域名/接口地址 | 数据库/云环境 | 备注 |
|---|---|---|---|---|
| 开发 |  | `http://本机或内网IP/api` |  | 微信开发者工具可勾选“不校验合法域名” |
| 测试 |  |  |  |  |
| 预发 |  |  |  |  |
| 正式 | main | `https://api.goomooplay.com/api` |  | 小程序 request 合法域名需配置 |

---

## 3. 本地启动与开发

### 3.1 本地启动方式

- **前端依赖安装命令：** `cd uniapp_623 && npm install`
- **微信小程序开发命令：** `npm run dev:mp-weixin`
- **微信小程序构建命令：** `npm run build:mp-weixin`
- **后端依赖安装命令：** `cd php && composer install --no-dev`
- **产物目录：** `uniapp_623/unpackage/dist/dev/mp-weixin` 或 `dist/build/mp-weixin`
- **需要的 Node / 包管理器版本：**
- **PHP 版本要求：** 8.0+
- **MySQL 版本要求：** 5.7+ 或 MariaDB 10.2+

### 3.2 本地开发注意事项

- **微信开发者工具版本要求：**
- **AppID：**
- **前端 baseURL 文件：** `uniapp_623/utils/request.js`
- **是否需要特定 hosts / 代理：**
- **本地调试常见问题：** 开发环境接口地址、合法域名校验、后端伪静态未配置、PHP 扩展缺失
- **线上后端实际部署目录（宿主机）：** `/root/deploy/php`

---

## 4. 发布流程

> 这是最关键的一部分。

### 4.1 当前发布方式

- [ ] 微信开发者工具手动上传
- [ ] CI/CD 自动上传
- [ ] 半自动（脚本 + 人工确认）
- [ ] 其他：

### 4.2 发布步骤

1. 备份数据库与 `public/uploads`
2. 拉取最新代码
3. 如后端依赖有变更，执行 `composer install --no-dev`
4. 如有数据库变更，执行 schema/ALTER 脚本
5. 按需清理 `php/runtime/cache`
6. 前端重新编译微信小程序版本
7. 使用微信开发者工具上传体验版/正式版
8. 提审 / 发布后做核心链路验证

### 4.3 提审流程

- **谁负责提审：**
- **提审前检查项：**
- **提审备注模板：**
- **审核被拒常见原因：**

### 4.4 上线流程

- **谁负责点发布：**
- **发布时间窗口：**
- **上线后检查项：**

### 4.5 回滚流程

- **是否支持快速回滚：**
- **回滚步骤：**
- **回滚权限人：**
- **回滚后验证方法：**

---

## 5. 配置与环境变量

> 建议只写“变量名 + 用途 + 存放位置”，不要直接把敏感值写进公开仓库。

| 变量名 | 用途 | 所属环境 | 存放位置 | 是否敏感 | 备注 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |
|  |  |  |  |  |  |
|  |  |  |  |  |  |

### 5.1 配置管理说明

- **前端配置文件位置：** `uniapp_623/utils/request.js`
- **服务端配置文件位置：** `php/.env`、`php/config/`
- **线上服务端配置文件（宿主机）：** `/root/deploy/php/.env`
- **数据库配置读取：** `php/config/database.php` 通过 `env('DB_xxx')` 读取
- **缓存配置：** `php/config/cache.php`（默认 file，可扩展 Redis）
- **日志配置：** `php/config/log.php`
- **哪些配置容易导致线上故障：** `DB_*`、`APP_DEBUG`、支付参数、微信/客服参数、request baseURL、合法域名、回调地址

---

## 6. 第三方平台与外部依赖

> 能影响线上运行的平台都列出来。

| 平台/服务 | 用途 | 登录地址 | 负责人 | 权限情况 | 备注 |
|---|---|---|---|---|---|
| 微信公众平台 | 小程序版本、合法域名、提审发布 |  |  |  | request 合法域名需含 `https://api.goomooplay.com` |
| 后端服务平台 | 部署 ThinkPHP 服务 |  |  |  | Web 根目录应指向 `php/public` |
| 数据库 | MySQL 业务数据 |  |  |  | 表前缀可能为 `mp_` |
| 对象存储/CDN | 静态资源/上传文件（如有） |  |  |  |  |
| 支付平台 | 汇付 / 微信支付 |  |  |  | 回调地址示例：`/api/order/notify` |
| 企业微信客服 | 在线客服能力 |  |  |  |  |
| 管家婆 | 订单同步 |  |  |  | 需配置 token 等参数 |
| 埋点/监控平台 | 监控错误、成功率、可用性 |  |  |  |  |
| 其他 |  |  |  |  |  |

---

## 7. 日志、监控与告警

### 7.1 日志入口

- **小程序前端错误日志查看位置：** 微信后台 / 前端埋点平台（待补）
- **云函数日志查看位置：** 当前文档未体现，若未使用可留空
- **后端服务日志查看位置：** `php/runtime/log/`
- **Nginx/Apache 日志查看位置：** 以服务器实际配置为准
- **数据库慢查询/异常查看位置：** MySQL 慢日志 / 平台监控（待补）

### 7.2 监控面板

- **核心监控面板地址：**
- **关键指标：**
  - 错误率：
  - 接口耗时：
  - 成功率：
  - 支付成功率：
  - 登录成功率：
  - 其他：

### 7.3 告警机制

- **告警渠道（飞书群/短信/邮件）：**
- **告警接收人：**
- **高优先级告警有哪些：**
- **夜间告警处理规则：**

---

## 8. 常见故障与排查 SOP

### 8.1 登录失败

- **常见原因：**
- **优先排查项：**
- **日志位置：**
- **处理方式：**

### 8.2 支付异常

- **常见原因：**
- **优先排查项：**
- **日志位置：**
- **处理方式：**

### 8.3 接口报错 / 5xx

- **常见原因：** 目录权限问题、PHP 扩展缺失、伪静态/重写配置错误、代码异常
- **优先排查项：** 检查 `php/runtime/log/`、检查 Nginx/Apache 重写、确认 PHP 版本与扩展
- **日志位置：** `php/runtime/log/`
- **处理方式：** 先看错误日志，再看 Web 服务器错误日志，确认 `php/public` 是否为站点根目录

### 8.4 云函数异常

- **常见原因：** 当前文档未体现明确云函数结构，需确认是否实际使用云函数
- **优先排查项：** 先确认问题是否实际发生在后端 PHP 服务，而不是云函数
- **日志位置：** 待补
- **处理方式：** 若当前项目主链路是 ThinkPHP 后端，则优先按后端服务故障处理

### 8.5 审核被拒 / 发布失败

- **常见原因：** 合法域名未配置、支付/客服能力配置不完整、提审说明不足、代码包问题
- **优先排查项：** 微信公众平台配置、request 合法域名、支付目录、客服参数、版本编译产物是否正确
- **处理方式：** 对照微信后台配置逐项核验，必要时回退到上一个稳定版本重新提交

### 8.6 其他高频问题

- **问题 1：** 订单未更新
- **问题 2：** 登录/支付失败
- **问题 3：** 接口 404 / 500

---

## 9. 权限边界

> 这一节决定“小微”能做什么，哪些必须先问你。

### 9.1 可直接处理

- [ ] 查看仓库代码
- [ ] 查看日志
- [ ] 排查问题
- [ ] 提交修复建议
- [ ] 提交 PR
- [ ] 修改文档
- [ ] 建任务 / 发汇报
- [ ] 其他：

### 9.2 需先确认后处理

- [ ] 合并 PR
- [ ] 发布测试环境
- [ ] 发布正式环境
- [ ] 回滚
- [ ] 修改生产配置
- [ ] 修改数据库
- [ ] 修改支付配置
- [ ] 其他：

### 9.3 禁止自动处理

- 
- 
- 

---

## 9.5 阿里云 / Docker 部署说明

### 服务器基础信息

- **云厂商：** 阿里云
- **主机名：** `iZbp1edbm4ajavhqp98srsZ`
- **操作系统：** Ubuntu 24.04.3 LTS
- **内核：** 6.8.0-90-generic
- **Docker 版本：** 28.2.2
- **Docker Compose 版本：** 2.37.1

### 当前容器结构

| 容器名 | 镜像 | 作用 | 端口/暴露 |
|---|---|---|---|
| `nginx-prod` | `nginx:1.18` | 对外 Web 入口 / 反向代理 | 宿主机 `8080 -> 容器80` |
| `php-prod` | `shanghaicatjump-prod-php:latest` | PHP-FPM / ThinkPHP 应用运行容器 | 容器内 `9000/tcp` |
| `mysql-prod` | `mysql:8.0` | MySQL 数据库 | 容器内 `3306/tcp`、`33060/tcp` |

### 代码与配置挂载关系

- **Nginx 配置挂载：**
  - 宿主机：`/root/deploy/nginx/default.conf`
  - 容器内：`/etc/nginx/conf.d/default.conf`

- **Nginx 对外静态目录挂载：**
  - 宿主机：`/root/deploy/php/public`
  - 容器内：`/var/www/html/public`

- **PHP 项目代码挂载：**
  - 宿主机：`/root/deploy/php`
  - 容器内：`/var/www/html`

### 当前运行方式判断

- `nginx-prod` 启动命令：`nginx -g 'daemon off;'`
- `php-prod` 启动命令：`php-fpm`
- 当前结构为 **Nginx + PHP-FPM 分容器部署**
- Nginx 容器通过配置把 PHP 请求转给 `php:9000`
- 项目代码目前位于宿主机 `/root/deploy/php`
- 当前未在 `/root/deploy` 下发现标准 `docker-compose.yml` / `compose.yml`，需确认是否为手工 `docker run` 启动，或 compose 文件存放在其他目录

### 对外访问链路（当前推断）

- 外部请求先到 **宿主机 8080**
- 再进入容器 **nginx-prod:80**
- PHP 动态请求再转发到 **php-prod:9000**
- MySQL 由容器 `mysql-prod` 提供

> 备注：如果线上域名最终走的是 80/443，再由宿主机 Nginx / 安全组 / 面板转到 8080，需要补充宿主机层的转发规则。

### 需要补充确认的信息

- **正式域名：**
- **HTTPS 终止位置：** 宿主机 / 容器 / CDN
- **是否还有宿主机 Nginx / 宝塔反代：**
- **MySQL 数据是否做宿主机目录挂载：**
- **日志目录具体位置：**
- **容器重启/发布命令：**
- **是否使用 docker compose 管理：**

### 常用排查点

- **Nginx 配置文件：** `/root/deploy/nginx/default.conf`
- **PHP 项目目录：** `/root/deploy/php`
- **前端公开目录：** `/root/deploy/php/public`
- **运行时日志目录：** `/root/deploy/php/runtime`
- **线上环境变量文件：** `/root/deploy/php/.env`
- **Composer 文件：** `/root/deploy/php/composer.json`
- **PHP 容器状态查看：** `docker ps`
- **Nginx 日志查看：** `docker logs -f nginx-prod`
- **PHP 日志查看：** `docker logs -f php-prod`
- **进入 Nginx 容器：** `docker exec -it nginx-prod sh`
- **进入 PHP 容器：** `docker exec -it php-prod sh`
- **Nginx 核心规则：** `root /var/www/html/public`，`try_files ... /index.php?s=$uri&$args`，PHP 转发到 `php:9000`

---

## 10. 日常运维节奏

### 10.1 每日检查

- **需要每天看的内容：**
- **检查时间：**
- **异常上报方式：**

### 10.2 每周检查

- **需要每周看的内容：**
- **例行维护项：**

### 10.3 发布前检查清单

- [ ] 代码已合并到正确分支
- [ ] 环境变量已确认
- [ ] 接口联调通过
- [ ] 核心页面已回归
- [ ] 支付/登录链路已验证
- [ ] 告警渠道正常
- [ ] 回滚方案已准备

---

## 11. 应急联系人与升级机制

| 场景 | 联系人 | 联系方式 | 备注 |
|---|---|---|---|
| 小程序发布问题 |  |  |  |
| 后端故障 |  |  |  |
| 数据库故障 |  |  |  |
| 支付问题 |  |  |  |
| 云平台问题 |  |  |  |
| 紧急决策人 |  |  |  |

### 升级规则

- **什么情况必须立即通知你：**
- **什么情况可以先排查后汇报：**
- **什么情况可以由小微直接处理：**

---

## 12. 交接后的第一阶段目标

### 第 1 阶段：能看懂
- [ ] 能读懂项目结构
- [ ] 能找到关键配置
- [ ] 能查看主要日志
- [ ] 能理解发布流程

### 第 2 阶段：能排查
- [ ] 能独立定位常见故障
- [ ] 能输出排查结论
- [ ] 能提出修复建议

### 第 3 阶段：能协助处理
- [ ] 能改文档
- [ ] 能提 PR
- [ ] 能跟踪发布
- [ ] 能做日常巡检

### 第 4 阶段：能半接管
- [ ] 能主持标准故障排查
- [ ] 能维护运维文档
- [ ] 能推动告警/巡检/复盘流程

---

## 13. 今天最小可填写版本

> 如果你今天时间不多，先只填下面这些。

- **仓库地址：**
- **项目简介：**
- **启动方式：**
- **发布方式：**
- **线上日志入口：**
- **监控面板入口：**
- **第三方平台清单：**
- **常见故障 3 个：**
- **谁能拍板上线/回滚：**
- **小微当前可直接做的事：**

---

## 14. 备注

- 
- 
- 
